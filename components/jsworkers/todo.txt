TODO:

Foxbox:
- Manage multiple clients connected to the same worker (in runtime.rs).
- Persistence in workers.rs
- Turn into an adapter?
- More tests.

Gecko:
- Create the worker with a per-usercontext principal.
- Make the logging look like foxbox's one.
- Tests.

Client library:
- Better Worker() api reimplementation.

All:
- webrtc mode.
